# nota

los archivos de acá son para el balanceador de carga, deben ser solo para los servicios que se puede acceder desde fuera del cluster mediante peticiones http directas. el profesor los creó el despliegue basado en google cloud, ademas, los nombres de los servicios que alli se describen `cliente-gateway` y `pagos-microservice-service`  deben existir, para confirmar podemos escribir en la terminal de docker, el comando `kubectl get services`
